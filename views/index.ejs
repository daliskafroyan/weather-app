<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Weather-Seeker</title>
  </head>
  <body>
    <div>
      <fieldset class="px-10 py-24 block">
        <form action="/" method="post">
          <input
            name="city"
            type="text"
            class="max-w-2xl px-5 py-1 mx-1 my-4 box-border block"
            placeholder="enter a city"
          />
          <br />
          <button
            type="submit"
            class="bg-blue-400 border-2 border-solid px-4 py-2 min-w-2 block"
          >
            check!
          </button>
        </form>

        <% if(weather !== null){ %>
        <p><%= weather %></p>
        <% } %> <% if(error !== null){ %>
        <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
